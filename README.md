# Personal Portfolio — Maheendra Menon

This is a minimal starter portfolio generated for `Maheendra Menon`.

How to preview locally

- Open `index.html` in your browser (double-click or right-click -> Open With).

Deploy to GitHub Pages

1. Create a new GitHub repository and push these files.
2. In repository Settings -> Pages, set the source to the `main` branch and `/ (root)` folder.
3. Save — your site will be available at `https://<username>.github.io/<repo>` shortly.

Next steps

- Provide email, social links, profile photo (place in `assets/`), and project details so I can populate the site.
- Tell me style preferences (colors, fonts, dark mode) if you want different looks.
